import { PlanPageContent } from '@/components/plan/plan-page-content';

export const metadata = {
  title: 'Study Plan — GMAT Prep',
};

export default function PlanPage() {
  return <PlanPageContent />;
}
