// Twitter/X reads twitter:image; reuse the exact same card as Open Graph so the
// preview is identical everywhere the link is shared.
export { default, size, contentType } from './opengraph-image';

export const alt = 'GMAT Prep - predict your GMAT Focus score, free';
